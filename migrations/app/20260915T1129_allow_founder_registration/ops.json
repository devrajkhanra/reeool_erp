[
  {
    "id": "alterNullability.dropNotNull.user.organizationId",
    "label": "Drop NOT NULL on \"user\".\"organizationId\"",
    "operationClass": "widening",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "organizationId",
        "table": "user"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"organizationId\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "user",
          "organizationId"
        ]
      }
    ],
    "execute": [
      {
        "description": "drop NOT NULL on \"organizationId\"",
        "sql": "ALTER TABLE \"public\".\"user\" ALTER COLUMN \"organizationId\" DROP NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"organizationId\" is nullable",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3 AND \"is_nullable\" = $4)) AS \"result\"",
        "params": [
          "public",
          "user",
          "organizationId",
          "YES"
        ]
      }
    ]
  }
]